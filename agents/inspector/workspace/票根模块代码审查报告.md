# 票根模块代码审查报告

**文件**: `/Volumes/SanDisk2T/dv-codeBase/茂名·交投-文旅平台/apps/backend/src/routes/mobile/ticket.js`
**总行数**: 789行

---

## 一、代码质量评级: **良好 (需优化)**

代码整体结构清晰，命名规范，业务逻辑正确。但存在单一文件承担过多功能的问题，建议拆分以提升可维护性。

---

## 二、必须拆分的问题点

### 1. 文件过大 (789行)
当前单文件承载9大功能模块:
- 票根类型 (types)
- 商户分类 (categories)  
- 首页轮播图 (banners)
- 热点资讯 (news)
- 用户票根 (my-tickets, recognize)
- 商户列表 (merchants)
- 商户详情 (merchant/:id)
- 核销码生成 (verify-code)
- 核销验证 (verify)

### 2. 重复代码模式
| 重复内容 | 出现位置 |
|---------|---------|
| 日期格式转换 `toISOString().split('T')[0]` | types, my-tickets, recognize, news |
| 分页参数校验 `Math.min(Math.max(parseInt(...), 1), 100)` | merchants, news |
| 响应格式化 `{ code: 200, data: ... }` | 全部9个接口 |
| try-catch 错误处理 | 全部9个接口 |

### 3. 字段映射重复
多处进行字段名转换 (如 `ticketType.code → type`, `cover_image → cover`)

---

## 三、具体拆分方案 (文件结构)

```
routes/mobile/ticket/
├── index.js              # 汇总导出 (~30行)
├── types.js              # 票根类型 (~80行)
├── categories.js         # 商户分类 (~60行)
├── banner.js             # Banner和资讯 (~130行)
├── user.js               # 用户票根管理 (~180行)
├── merchants.js          # 商户列表+详情 (~180行)
├── verify.js             # 核销相关 (~150行)
└── utils.js               # 公共工具 (~60行)
```

**建议每个文件不超过 150-200行**

---

## 四、各文件行数建议

| 文件 | 建议行数 | 说明 |
|-----|---------|------|
| index.js | 30 | 路由汇总导出 |
| types.js | 80 | 仅2个接口 |
| categories.js | 60 | 仅1个接口 |
| banner.js | 130 | banners + news |
| user.js | 180 | my-tickets + recognize |
| merchants.js | 180 | merchants + merchant/:id |
| verify.js | 150 | verify-code + verify |
| utils.js | 60 | 公共函数 |

---

## 五、其他改进建议

### 1. 抽取公共模块
```javascript
// utils/response.js - 统一响应格式
res.json({ code: 200, data: ... })

// utils/pagination.js - 分页处理
const validPageSize = Math.min(Math.max(parseInt(pageSize) || 10, 1), 100)

// utils/date.js - 日期格式化
formatDate(date) => date.toISOString().split('T')[0]
```

### 2. 添加类型定义 (建议迁移 TypeScript)
- 定义 Request/Response DTO
- 为 Prisma 模型添加类型推断

### 3. 服务层抽象 (可选)
将业务逻辑从路由处理函数中分离，提高可测试性:
```
services/ticketService.js
services/merchantService.js
services/verificationService.js
```

### 4. 错误处理优化
- 封装统一错误中间件
- 参数校验可使用 `zod` 或 `joi`

---

## 六、总结

| 指标 | 现状 | 建议 |
|-----|-----|-----|
| 文件大小 | 789行/单文件 | ≤150-200行/文件 |
| 功能模块 | 9个混合 | 按功能拆分 |
| 代码复用 | 多处重复 | 抽取工具函数 |
| 可测试性 | 紧耦合 | 抽象服务层 |
| 类型安全 | 无 | 引入 TypeScript |

**建议优先级**: 拆分文件 > 抽取公共函数 > 错误处理优化
